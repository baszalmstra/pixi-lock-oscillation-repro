[project]
name = "root"
requires-python = ">= 3.11"
version = "0.1.0"
dependencies = ["c"]

[tool.uv.sources]
c = { path = "../sub/c" }

[tool.pixi.pypi-dependencies]
root = { path = "." }

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]
