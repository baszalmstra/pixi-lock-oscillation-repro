[project]
name = "c"
requires-python = ">= 3.11"
version = "0.1.0"
dependencies = ["b"]

[tool.uv.sources]
b = { path = "../b" }

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]
